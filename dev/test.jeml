% 'use strict';

// import layout "./layout"
// import body   "./body"
// import { "ddddd" as f, z, f, 'fff' as f, k as j, default as k } "./something"

// import { xyz: 'abc', efg: 'okk' } from './abc'

% var x = require('./abc')

export default function() {
    if a {
        input(type="name")
        "hello, I'm feeling good
        how are you"
        [/hi/gi] // hi
        [x === 1 || okk || yes && no + 10 ? y ? ss : x + -1.2 : z ? f : f]
    else
        [new Date().now || 10 instanceof abc ? abb : fff ? abc : dd]
    }

    (new Data)

    for x of y {

    }

    switch xxx {
    case x > 10:

    }

    each name, b in abc {
        +(name: d,x,z)

        #(name: x, y, z)
        @(expression: x, y, z)
        expression.call(@)
    }

    each arg in arguments {
        # console.log(arg)
    }

    case {
    when x > 10:

    when y > 20:

    }

    switch {
    case x > 10:

    case y > 20:

    else
        // ...
        // ...
    }

    switch x {
    case a, b, c:

    case e, f, g:

    else

    }
}

export
function abc(t) {
    html {
        head: title: &(t.title)
        body: p: %(t.text)
    }
}

export
function name2(a1, a2) {
	okk: "dd"
}
